[font]
normal = { family = "JetbrainsMono Nerd Font Mono", style = "Regular"}

[keyboard]
bindings = [
   { key = "Return", mods = "Control|Shift", action = "SpawnNewInstance" } # CTRL+SHIFT+ENTER spawns new terminal in same working directory
]
